cmake_minimum_required(VERSION 2.8)
project( harris_detector )
find_package( OpenCV REQUIRED )
add_executable( harris_detector harris_detector.cpp )
target_link_libraries( harris_detector ${OpenCV_LIBS} )
